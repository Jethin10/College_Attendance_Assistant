{
  "name": "niet-attendance-extension",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/copy-static.mjs && node scripts/build-content.mjs && vite build --watch",
    "build": "vite build && node scripts/build-content.mjs && node scripts/copy-static.mjs",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.25.9",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.3",
    "vite": "^6.3.1"
  }
}
